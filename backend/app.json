{
  "name": "bolao-backend",
  "description": "Backend Django — Bolão Copa do Mundo 2026",
  "keywords": ["django", "python", "bolao"],
  "env": {
    "SECRET_KEY": {
      "description": "Django secret key — gere com: python -c \"import secrets; print(secrets.token_urlsafe(50))\"",
      "required": true
    },
    "DEBUG": {
      "description": "False em produção",
      "value": "False"
    },
    "ALLOWED_HOSTS": {
      "description": "Domínios permitidos separados por vírgula",
      "required": true
    },
    "CORS_ALLOWED_ORIGINS": {
      "description": "URL do frontend separadas por vírgula",
      "required": true
    },
    "DB_NAME": { "required": true },
    "DB_USER": { "required": true },
    "DB_PASSWORD": { "required": true },
    "DB_HOST": { "required": true },
    "DB_PORT": { "value": "5432" }
  }
}
